<section class="header">
  <p class="page-tab-active page-tab" id="tab-all">Pour vous</p>
  <p class="page-tab" id="tab-following">Abonnements</p>

   <form id="logout-form" action="{{ route('logout') }}" method="POST" style="display: none;">
    <input type="hidden" name="_csrf" value="{{ csrfToken }}">
  </form>
  <!-- Ajoute un lien vers le profil de l'utilisateur connecté dans le header ou le menu -->
  <a href="#" id="logout-link">Logout</a>
</section>
